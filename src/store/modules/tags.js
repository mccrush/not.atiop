export default {
  state: {
    tags: []
  },

  mutations: {},

  actions: {
    async addITag({ commit }, { item, currentUserId }) {

    },

    async getTags({ commit }, { currentUserId }) {

    },
  },

  getters: {
    tags: state => state.tags
  }
}